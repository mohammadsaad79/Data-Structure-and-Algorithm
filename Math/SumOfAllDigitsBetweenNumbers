#include<bits/stdc++.h>
using namespace std;

int calculateDigitSum(int N1, int N2) {
	int sum = 0;
	int r;
	int num;
	for(int i=N1; i<=N2; i++) {
	    num = i;
	    while(num!=0) {
	        r = num%10;
	        sum += r;
	        num /= 10;
	    }
	}
	return sum;	
}

int main() {
    int n1, n2;
    cout << "Enter first digit: ";
    cin >> n1;
    cout << "Enter second digit: ";
    cin >> n2;
    cout << calculateDigitSum(n1, n2);
    return 0;
}
